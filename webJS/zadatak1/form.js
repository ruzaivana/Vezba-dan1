function crveniform() {
   var unoselement = document.getElementById('input');
   
   if (unoselement.hasAttribute('required') ) {
       
    unoselement.className = ('redB');
            // unoselement.setAttribute('');
        
//    } else {
       
   }

}